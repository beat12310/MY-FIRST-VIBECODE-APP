version: 1
# npm ci (not npm install) fails in this build environment: this session
# added vitest/husky/zod to package.json, and vitest's transitive vite
# dependency pulls in a nested esbuild version that npm ci's strict
# byte-exact lockfile validation rejects here (and in GitHub Actions'
# runner) even though the SAME lock file installs cleanly with npm ci on a
# local Mac — confirmed live: this exact "Missing: esbuild@0.28.1 from lock
# file" error on Amplify's CodeBuild-based build environment, root-caused
# and already fixed the same way for .github/workflows/ci.yml and
# scheduled-verification.yml. npm install still installs deterministically
# from the committed lock file; it's just tolerant of this specific
# dependency tree's cross-environment resolution difference where npm ci
# is not.
#
# node_modules/.npm removed from cache.paths (and --cache/--prefer-offline
# removed from both npm install calls) after two consecutive real
# deployment attempts (jobs 22, 23) failed identically on "npx ampx
# pipeline-deploy" -> "npm error could not determine executable to run",
# with the exact same suspiciously-low "audited 618 packages" count both
# times despite package.json declaring far more — consistent with a stale/
# corrupted cached node_modules being restored rather than a fresh install.
# There's no AWS CLI command to directly clear Amplify's build cache (only
# a Console UI toggle); forcing every build to install from scratch is the
# verifiable alternative.
backend:
  phases:
    build:
      commands:
        - npm install
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
